<!---
This file was automatically generated by Mastro CLI
Generated on: 2025-08-11T02:41:21.448Z
Document type: api
Title: API Documentation

To prevent this file from being overwritten, add custom content
between the CUSTOM_START and CUSTOM_END markers below.
--->

# Mastro API Documentation

## Overview

Mastro is an enterprise-level API designed for robust data analysis and processing. Built with TypeScript and Node.js, Mastro provides a set of functions that allow users to analyze documents, manage commands, and handle streaming data efficiently. The architecture emphasizes modularity, scalability, and ease of integration.

### Design Principles
- **Modularity**: Each component is designed to perform a specific function, making it easy to maintain and extend.
- **Asynchronous Processing**: Many functions support asynchronous operations to enhance performance and responsiveness.
- **Error Handling**: Comprehensive error handling mechanisms are in place to ensure reliability.

## Authentication and Authorization

Currently, Mastro does not require authentication or authorization for its API endpoints. Future versions may include these features to enhance security.

## Detailed Endpoint Documentation

### 1. Document Analyzer

#### `scanRecursive`

- **Signature**: `const scanRecursive = (currentPath, basePath = '') =>`
- **Description**: Recursively scans files in a directory.
- **Complexity**: Low
- **Async**: No

**Example Usage**:
```javascript
const { scanRecursive } = require('mastro/lib/analyzers/doc-analyzer');

const files = scanRecursive('/path/to/directory');
console.log(files);
```

### 2. Command Operations

#### `operation`

- **Signature**: `operation: () =>`
- **Description**: Executes a predefined operation.
- **Complexity**: Low
- **Async**: No

**Example Usage**:
```javascript
const { operation } = require('mastro/lib/base/command');

operation();
```

### 3. Analytics Commands

#### `exitHandler`

- **Signature**: `const exitHandler = async () =>`
- **Description**: Handles exit events asynchronously.
- **Complexity**: Low
- **Async**: Yes

**Example Usage**:
```javascript
const { exitHandler } = require('mastro/lib/commands/analytics');

process.on('exit', exitHandler);
```

### 4. Flow Commands

#### `readDirRecursive`

- **Signature**: `const readDirRecursive = async (dir) =>`
- **Description**: Reads a directory recursively.
- **Complexity**: Low
- **Async**: Yes

**Example Usage**:
```javascript
const { readDirRecursive } = require('mastro/lib/commands/flow');

const files = await readDirRecursive('/path/to/directory');
console.log(files);
```

#### `action`

- **Signature**: `action: async () =>`
- **Description**: Executes a specific action.
- **Complexity**: Low
- **Async**: Yes

**Example Usage**:
```javascript
const { action } = require('mastro/lib/commands/flow');

await action();
```

### 5. Review Commands

#### `chunkHandler`

- **Signature**: `chunkHandler: (chunk) =>`
- **Description**: Processes a chunk of data.
- **Complexity**: Low
- **Async**: No

**Example Usage**:
```javascript
const { chunkHandler } = require('mastro/lib/commands/review');

chunkHandler(dataChunk);
```

### 6. Core Functions

#### `isDocs`

- **Signature**: `const isDocs = (file) =>`
- **Description**: Checks if a file is a document.
- **Complexity**: Low
- **Async**: No

**Example Usage**:
```javascript
const { isDocs } = require('mastro/lib/core/ai-client');

const result = isDocs('example.docx');
console.log(result);
```

#### `exists`

- **Signature**: `const exists = async (p) =>`
- **Description**: Checks if a path exists asynchronously.
- **Complexity**: Low
- **Async**: Yes

**Example Usage**:
```javascript
const { exists } = require('mastro/lib/core/ai-client');

const pathExists = await exists('/path/to/file');
console.log(pathExists);
```

## Request/Response Schemas

### Example Request Schema
```json
{
  "path": "/path/to/directory"
}
```

### Example Response Schema
```json
{
  "files": [
    "file1.txt",
    "file2.txt"
  ]
}
```

## Error Handling and Status Codes

Mastro uses standard HTTP status codes for error handling:

- **200 OK**: Request succeeded.
- **400 Bad Request**: Invalid request parameters.
- **404 Not Found**: Resource not found.
- **500 Internal Server Error**: Unexpected server error.

### Example Error Response
```json
{
  "error": "Invalid request parameters."
}
```

## Rate Limiting and Usage Guidelines

Currently, there are no rate limits imposed on the API. However, users are encouraged to implement their own throttling mechanisms to avoid overwhelming the server.

## SDK/Client Library Information

Mastro does not currently offer an official SDK. However, users can easily integrate the API using standard HTTP requests or by importing the necessary modules directly in their Node.js applications.

## Code Examples in Multiple Languages

### JavaScript Example
```javascript
const { readDirRecursive } = require('mastro/lib/commands/flow');

async function listFiles() {
    const files = await readDirRecursive('/path/to/directory');
    console.log(files);
}

listFiles();
```

### Python Example (using requests)
```python
import requests

response = requests.get('http://api.mastro.io/readDirRecursive', params={'dir': '/path/to/directory'})
print(response.json())
```

## Testing and Development Guidelines

1. **Local Development**: Clone the repository and run `npm install` to set up the environment.
2. **Testing**: Use Jest for unit testing. Run `npm test` to execute the test suite.
3. **Documentation Updates**: Ensure that any changes to the API are reflected in this documentation to maintain accuracy.

For further inquiries or contributions, please refer to the project's GitHub repository.

---

<!-- CUSTOM_START -->
<!-- Add your custom content here - it will be preserved during regeneration -->
<!-- CUSTOM_END -->

*Documentation generated by [Mastro CLI](https://github.com/your-org/mastro) on 8/10/2025*